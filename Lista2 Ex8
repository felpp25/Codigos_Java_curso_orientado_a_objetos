public static void main(String[] args) {
	Scanner ler = new Scanner (System.in);
		double fun1, fun2, fun3, fun4, fxy;
		int x, y, codigo;
		x = 0;
		y = 0;
		codigo = 0;
		
		System.out.print("Informe o valor de X sendo este maior que 0: ");
		x = ler.nextInt();
		System.out.print("\n");
		System.out.print("Informe o valor de Y sendo este maior que 0: ");
		y = ler.nextInt();
		
		System.out.print("O sistema devera calcular a f(x,y) de acordo com o CODIGO de 1 a 4 que usuario solictar!");
		System.out.print("\n");
		System.out.println("(1)- 𝑓(𝑥, 𝑦) = √(𝑥3 +(x(2𝑦 − 𝑥)))." );
		System.out.println("(2)-𝑓(𝑥, 𝑦) =√((𝑥3 + 𝑦3) + (𝑥3 − 𝑦3)) /𝑥 ∗ y." );
		System.out.println("(3)-𝑓(𝑥, 𝑦) = 2𝑥 + √𝑥 ∗ y." );
		System.out.println("(4)-𝑓(𝑥, 𝑦) = sin(𝑥) + cos(𝑦)." );
		System.out.print("\n");
		System.out.print("informe o codigo da f(x,y): ");
		codigo = ler.nextInt();
		
		if (codigo < 0 || codigo > 4 ){
		    System.out.print("Codigo da funcao invalido:\n ");
		    System.out.print("Informe o codigo de 1 a 4:\n ");
		    codigo = ler.nextInt();
		    System.out.print("\n");
		}
		
		switch (codigo) {
		case 0: 
			break;
		case 1:
		    fxy = 
			break;
		case 2:
			fxy = 
			break;	
		case 3:
		    fxy = 
			break;	
		case 4:
			fxy = 
			break;	
	    }
	
	    System.out.print("Resultado da f(x,y): "+fxy);

}
